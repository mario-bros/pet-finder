version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: baja_pet_rescue_development
    volumes:
      - db_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

#  web:
#    build: .
#    command: bundle exec rails server -b 0.0.0.0
#    volumes:
#      - .:/app
#    ports:
#      - "3000:3000"
#    depends_on:
#      - db
#    environment:
#      RAILS_ENV: development
#      DATABASE_HOST: db
#      DATABASE_USER: postgres
#      DATABASE_PASSWORD: password
#      DATABASE_NAME: baja_pet_rescue_development
volumes:
  db_data:
